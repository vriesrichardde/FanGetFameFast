# FanGetFameFast — Python package requirements
# Install into the project virtual environment:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# Versions are pinned (==) to the exact releases recorded in the SBOM
# (sbom.json / sbom.md). Regenerate the SBOM after any change here:
#   python3 scripts/generate_sbom.py

# PDF generation
weasyprint==68.1
cairocffi==1.7.1
CairoSVG==2.9.0

# Office document generation
python-pptx==1.0.2
python-docx==1.2.0

# Markdown processing
Markdown==3.10.2

# Data / config parsing
PyYAML==6.0.3

# Interactive timeline visualisation (HTML output)
plotly==5.18.0

# Memory forensics (FAME)
volatility3==2.0.0
yara-python==4.3.1

# TLS / certificate inspection (FAN)
sslyze==5.0.0

# MemProcFS physical memory analysis (FAME — optional, x86-64 only)
# May not install on arm64; post-create.sh skips gracefully on failure.
memprocfs==5.0.0
